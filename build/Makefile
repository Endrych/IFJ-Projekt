PROJECT = ifj17
PROJECT_TEST = $(PROJECT)-test
BIN_FOLDER = bin/
SRC_FOLDER = ../src/
TESTS_FOLDER = ../tests/

CC = gcc
RM = rm -rf

STD_C99 = -std=c99
CFLAGS = -Wall -Wextra -pedantic

all:
	$(CC) $(STD_C99) $(CFLAGS) $(SRC_FOLDER)main.c -o $(PROJECT)